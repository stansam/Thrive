
// Placeholder to prevent breaking imports if any, but logic is moved to FlightsTab
import { redirect } from 'next/navigation';

export default function MyBookingsTab() {
    return <div>Use Flights tab to manage bookings.</div>;
}
